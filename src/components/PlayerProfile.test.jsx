import PlayerProfile from "./PlayerProfile";

describe("PlayerProfile Component", () => {
  it("deve renderizar o nome de utilizador corretamente na tag h2", () => {
    expect.hasAssertions();
    // TODO: Usar @testing-library/react (render e screen) para verificar se o <h2> contém o texto da prop username.
    throw new Error("teste ainda nao implementado pelo aluno.");
  });
});
