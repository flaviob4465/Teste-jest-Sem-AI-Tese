import renderer from "react-test-renderer";

import PlayerProfile from "./PlayerProfile";

describe("PlayerProfile Component", () => {
  it("deve renderizar level e rank nas tags p", () => {
    expect.hasAssertions();
    // TODO: Usar renderer.create(...) e root.findAllByType("p") para validar "Level: ..." e "Rank: ...".
    throw new Error("teste ainda nao implementado pelo aluno.");
  });
});
